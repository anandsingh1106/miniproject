# Core web stack — this is all you need to run the site.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4

# Imaging + classical-CV detection engine (the zero-download baseline)
numpy==2.2.1
opencv-python-headless==4.10.0.84
pillow==11.1.0
