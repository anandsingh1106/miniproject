# Runtime data — SQLite DB, uploaded imagery, trained weights
data/
!data/.gitkeep

# Training artefacts
runs/
*.pt

# npm packages. frontend/vendor/ IS committed (see scripts/vendor.js) so a
# fresh clone runs with python alone — npm is only needed to update assets.
node_modules/
package-lock.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
